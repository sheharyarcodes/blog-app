import React from "react";
import { Container, Login as LoginComponent } from "@/components";
const Login = () => {
  return (
    <Container>
      <LoginComponent />;
    </Container>
  );
};

export default Login;
